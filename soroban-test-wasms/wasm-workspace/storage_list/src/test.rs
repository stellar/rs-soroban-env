#![cfg(test)]

use super::*;
use soroban_sdk::{vec, Env};

// #[test]
// fn test() {
//     let env = Env::default();
//     let contract_id = env.register_contract(None, Test);
//     let client = TestClient::new(&env, &contract_id);

//     client.set_list(&vec![1u64, 2u64]);
// }
